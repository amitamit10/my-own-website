# Custom Water Font

`water.woff2` in this folder is copied from `tools/font-builder/output/water.woff2`.

If the font is rebuilt during later font work, refresh this site copy before shipping:

```bash
cp tools/font-builder/output/water.woff2 public/fonts/water.woff2
```
